# Fingerprint-Image-Extractor-R307S-
This project demonstrates a complete pipeline for capturing fingerprint images using an ESP32 and an R307 fingerprint sensor, then converting the raw data into a viewable PNG image using Python.
